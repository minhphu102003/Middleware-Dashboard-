{
  "name": "middleware",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm-run-all --parallel start:backend start:frontend",
    "start:backend": "cd backend && npm start",
    "start:frontend": "cd middleware && yarn serve"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "npm-run-all": "^4.1.5"
  }
}
